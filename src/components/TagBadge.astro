---
interface Props {
  name: string;
  slug: string;
}

const { name, slug } = Astro.props;
---
<a
  href={`/tags/${slug}`}
  class="inline-block border border-outline text-ink-muted font-mono uppercase text-xs px-2 py-0.5 hover:bg-ink hover:text-surface-raised transition-colors"
>
  {name}
</a>
