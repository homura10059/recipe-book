---
interface Props {
  name: string;
  slug: string;
}

const { name, slug } = Astro.props;
---
<a
  href={`/categories/${slug}`}
  class="inline-block border border-accent text-accent text-xs px-2 py-0.5 hover:bg-accent hover:text-white transition-colors tracking-wide"
>
  {name}
</a>
